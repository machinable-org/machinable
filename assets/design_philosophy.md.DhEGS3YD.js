import{_ as a,o as t,c as i,ag as n}from"./chunks/framework.Nx1zvZTu.js";const u=JSON.parse('{"title":"Philosophy","description":"","frontmatter":{},"headers":[],"relativePath":"design/philosophy.md","filePath":"design/philosophy.md"}'),o={name:"design/philosophy.md"};function r(s,e,h,d,l,c){return t(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="philosophy" tabindex="-1">Philosophy <a class="header-anchor" href="#philosophy" aria-label="Permalink to &quot;Philosophy&quot;">​</a></h1><p>These notes record the reasoning behind machinable. The <a href="/guide/introduction.html">guide</a> explains how to use it; this section explains why it is built the way it is.</p><h2 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h2><p>Research code accumulates housekeeping. A project starts with an algorithm and ends up maintaining configuration plumbing, a results directory convention, a way to avoid re-running finished work, a sweep harness, a place to put metrics, and glue to read everything back. None of that is the research, and every project reinvents it slightly differently.</p><p>machinable&#39;s bet is that all of this housekeeping can be handled by one abstraction, provided you accept a single discipline: <strong>running code and retrieving its results are the same operation</strong>. You write your research as an <a href="/guide/interface.html"><code>Interface</code></a> with a typed <a href="/guide/configuration.html"><code>Config</code></a>, and <code>get(...)</code> means both &quot;run this&quot; and &quot;give me what this produced&quot;. Because runs are <a href="/guide/identity.html">content-addressed</a>, asking for the same thing twice is free and sweeps are incremental.</p><h2 id="code-all-the-way-down" tabindex="-1">Code all the way down <a class="header-anchor" href="#code-all-the-way-down" aria-label="Permalink to &quot;Code all the way down&quot;">​</a></h2><p>machinable is an API, not a framework that owns your workflow. There is deliberately no metrics schema, no <code>log_metric</code> call, and no <code>sweep()</code> DSL. A grid is a Python loop, a measurement is a method, a statistical question is an <a href="./inference.html">inference</a>. machinable supplies only what is genuinely cross-cutting: identity, deduplication, storage, and search.</p><p>The reason is that schemas and DSLs try to anticipate what researchers will need, and research is precisely the activity of doing something unanticipated. Every convenience layer we considered (a metrics format, a sweep helper) constrained more than it helped; researchers wrote the Python anyway and then fought the abstraction. The rule throughout is to surface existing primitives rather than invent new ones.</p><p>This stance is also what makes machinable drivable by an <a href="/mcp/overview.html">AI agent</a>, because if the workflow is code, an agent that writes code can run the experiment.</p><h2 id="content-addressing-as-the-backbone" tabindex="-1">Content-addressing as the backbone <a class="header-anchor" href="#content-addressing-as-the-backbone" aria-label="Permalink to &quot;Content-addressing as the backbone&quot;">​</a></h2><p>A run is identified by its content (its module and canonical configuration), not by a name, a path, or a timestamp. This is what lets <code>get</code> recognize prior work, makes sweeps incremental, and makes results reproducible by construction. Much of the harder design work (<a href="./identity.html">identity</a>, <a href="./storage.html">storage</a>) is about getting content-addressing right, i.e. robust to how a config is spelled, where its data lives, and how the schema evolves.</p><h2 id="extensibility-is-sharing-not-registration" tabindex="-1">Extensibility is sharing, not registration <a class="header-anchor" href="#extensibility-is-sharing-not-registration" aria-label="Permalink to &quot;Extensibility is sharing, not registration&quot;">​</a></h2><p>There is no plugin registry. An execution backend, a storage adapter, a statistical inference, a widget are all are ordinary Python importable modules. A generally useful one is shared (pulled into a project by URL via <code>on_resolve_remotes</code>, like the <code>slurm</code> and <code>globus</code> <a href="/integrations/">integrations</a>); a one-off lives in the project as a throwaway module. Extending machinable and using it are the same activity.</p><h2 id="legibility" tabindex="-1">Legibility <a class="header-anchor" href="#legibility" aria-label="Permalink to &quot;Legibility&quot;">​</a></h2><p>Finally, the system optimizes for a human or an agent being able to understand a result. Compact <a href="/guide/versions.html"><code>~versions</code></a> name experiments the same way in the CLI, the API, and the MCP; verdicts carry their provenance; any result can be rendered as a <a href="/guide/widgets.html">widget</a>. An experiment you cannot read is one you cannot trust.</p>',15)])])}const g=a(o,[["render",r]]);export{u as __pageData,g as default};
