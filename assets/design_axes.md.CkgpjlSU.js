import{_ as t,o,c as a,ag as n}from"./chunks/framework.Nx1zvZTu.js";const p=JSON.parse('{"title":"Axes","description":"","frontmatter":{},"headers":[],"relativePath":"design/axes.md","filePath":"design/axes.md"}'),i={name:"design/axes.md"};function s(r,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="axes" tabindex="-1">Axes <a class="header-anchor" href="#axes" aria-label="Permalink to &quot;Axes&quot;">​</a></h1><p>Why fan-out (<code>axis_&lt;name&gt;</code> → <a href="/guide/versions.html#axes-one-token-many-runs"><code>~~name</code></a>) is a version token rather than a second module, a decorator, or a collection.</p><h2 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to &quot;The problem&quot;">​</a></h2><p>Running one interface over many inputs used to cost a second module whose entire body was a <code>for</code> loop. The <a href="/guide/advanced-execution.html#aggregates-and-deferred-collection">aggregate</a> is right when the coordinator has a config of its own, but for &quot;sort these 40 sessions&quot; it was boilerplate — and, worse, invisible. The CLI, the API and the MCP all speak versions, so a sweep that lived in a Python method could not be named on any of them.</p><p>The unused degree of freedom was that a version token denoted exactly one config. Letting a token denote <em>several</em> turns the version list into a product space without inventing a configuration language: an axis is still an ordinary Python function returning a list.</p><p>A useful consequence: axes resolve on the interface <em>and</em> on the project provider, like config methods. A provider-level <code>axis_sessions</code> is a project-wide vocabulary that every module can expand over.</p><h2 id="cardinality-belongs-to-the-call-site" tabindex="-1">Cardinality belongs to the call site <a class="header-anchor" href="#cardinality-belongs-to-the-call-site" aria-label="Permalink to &quot;Cardinality belongs to the call site&quot;">​</a></h2><p><code>~~</code> is not decoration. It is the difference between <code>get(...)</code> returning a record and returning a group, and that difference has to be visible everywhere a version is — <code>to_cli()</code>, a stored <code>_version_</code>, an MCP call, a line pasted into a notebook.</p><p>The alternative considered was an <code>@axis</code> decorator on ordinary <code>version_*</code> methods, so that <code>[&#39;~sgd&#39;, &#39;~sweep&#39;]</code> expands without a distinct sigil. It is cheaper (one method family, no parser change) and it was rejected: <code>~sweep</code> and <code>~fast</code> would look identical while one launches 200 runs, and an author flipping <code>version_sweep</code> from a patch to a list would silently change every existing call site from producing a record to producing a group — including the lifecycle state the returned object is in. Cardinality is part of a call site&#39;s contract, not an implementation detail of the method it names. (Secondary, but concrete: <a href="./discovery.html">discovery</a> is AST-only and never imports user code, so a prefix is unfoolable where a decorator must be resolved through the import table, aliases included.)</p><h2 id="unexpanded-is-a-state-not-a-subtraction" tabindex="-1">Unexpanded is a state, not a subtraction <a class="header-anchor" href="#unexpanded-is-a-state-not-a-subtraction" aria-label="Permalink to &quot;Unexpanded is a state, not a subtraction&quot;">​</a></h2><p>An interface with a pending axis is the module&#39;s own class, in a new <em>leading</em> lifecycle state. It can be expanded, launched, versioned and printed; it can never be entered, called, or materialized.</p><p>Defining a thing by what it cannot do is a Liskov smell, and the objection is worth recording because the resolution is the interesting part: this is not subtraction, it is the same shape as <code>local_directory()</code> on an interface that is not yet materialized. <code>expand()</code> is a transition exactly as <code>materialize()</code> is, and the <a href="/guide/interface.html#lifecycle">lifecycle table</a> simply gains a row.</p><p>Keeping the module&#39;s own class (rather than returning a collection or a new kind) is what preserves the class&#39;s methods — a group-level <code>summary()</code>, the quantity accessor an <a href="/guide/inference.html">inference</a> falls back to — keeps <code>get() -&gt; Interface</code>, and keeps <code>get()</code> lazy: nothing is globbed until you launch or ask.</p><h2 id="the-silent-collapse-hazard" tabindex="-1">The silent-collapse hazard <a class="header-anchor" href="#the-silent-collapse-hazard" aria-label="Permalink to &quot;The silent-collapse hazard&quot;">​</a></h2><p>The guards exist because of one specific failure. <code>Scope.__call__</code> returns its config update, and an unexpanded scope&#39;s update does <em>not</em> contain the axis patch — so <code>with get(&quot;machinable.scope&quot;, &quot;~~seeds&quot;):</code> would apply an <strong>empty predicate</strong> and fold an entire sweep onto one record, without an error. Blocking <code>__enter__</code> makes that unreachable.</p><p>The same class of failure has a general net: since every member&#39;s <code>(identity_key, predicate_key)</code> pair is computed anyway (below), an expansion that yields N elements but fewer than N distinct pairs is rejected. That single check catches a non-<code>Scope</code> context, an axis method that ignores its loop variable, duplicate paths from a sloppy glob, and a version override positioned after the axis token that cancels out the element&#39;s key. It is the reason no whitelist of forbidden element types is needed.</p><h2 id="contexts-must-be-predicate-bearing" tabindex="-1">Contexts must be predicate-bearing <a class="header-anchor" href="#contexts-must-be-predicate-bearing" aria-label="Permalink to &quot;Contexts must be predicate-bearing&quot;">​</a></h2><p>An element is a config patch or a <strong><code>Scope</code></strong> — nothing else. Entering an arbitrary interface is legal Python but its effect is kind-dependent: a <code>Scope</code> folds into the predicate, an <code>Execution</code> re-routes where members queue, a <code>Storage</code> re-routes where records land, and a plain interface does <em>nothing</em> except leave an identity-neutral context record. So <code>axis_datasets</code> yielding <code>[get(&quot;dataset_a&quot;), get(&quot;dataset_b&quot;)]</code> reads like it expands over datasets and instead produces N identical members.</p><p>The rule is narrower than it sounds only in name: <code>kind</code> is inherited, so any <code>Scope</code> subclass qualifies, with its own <code>Config</code> and a <code>__call__</code> returning a richer predicate.</p><p><strong>Executions are deliberately not elements.</strong> The semantics does not resolve: entering one per member gives a fresh execution per run (right for Slurm, absurd for a multiprocessing pool), while grouping members by execution requires identity-based election among objects machinable never deduplicates, since each execution is a distinct event. And the motivating case is already a first-class hook — resources are computed <em>per interface</em> (<code>on_compute_default_resources(interface)</code>), so size-dependent resources are one execution with per-member resources, not an axis element. Storages are excluded for a further reason: members in different stores share <code>(identity_key, predicate_key)</code>, so duplicate detection would reject a legitimate sweep; adding them would first require widening that key with the parent.</p><h2 id="identity" tabindex="-1">Identity <a class="header-anchor" href="#identity" aria-label="Permalink to &quot;Identity&quot;">​</a></h2><p>Two rules, pulling in opposite directions.</p><p><strong>Members are identity-neutral.</strong> A run launched through an axis is the same record as the same configuration launched directly — no group tag, no extra predicate. Axes are UX, like <a href="/guide/versions.html#versions-are-ux-not-identity">versions</a>; version spelling never reaches the hash, since the <code>_version_</code> layer is popped out of the resolved config before canonicalization.</p><p><strong>The group is identified by the set it expands into</strong>, as the sorted <code>(identity_key, predicate_key)</code> pairs of its members. Config identity alone is not enough: members that differ only by scope share a config, so <code>~~seeds(10)</code> and <code>~~seeds(3)</code> would otherwise be the same <a href="/guide/inference.html">inference</a> operand. The pairs must be captured while each member&#39;s contexts are entered — <code>compute_predicate</code> reads the live connection stack, so reading a member&#39;s predicate after its scope has exited returns the ambient one.</p><p>An unexpanded interface also never participates in record lookup (<code>singleton</code> short-circuits, and <code>compute_fingerprint()</code> returns <code>None</code> as an <code>Execution</code> does). Without that, <code>get(&quot;train&quot;, [&quot;~sgd&quot;, &quot;~~seeds&quot;])</code> would resolve onto the <em>existing plain</em> <code>~sgd</code> record, whose base config it shares.</p><h2 id="an-axis-is-a-pure-function-of-its-arguments" tabindex="-1">An axis is a pure function of its arguments <a class="header-anchor" href="#an-axis-is-a-pure-function-of-its-arguments" aria-label="Permalink to &quot;An axis is a pure function of its arguments&quot;">​</a></h2><p><code>axis_*</code> must be a <code>@staticmethod</code>. The rule it replaced — &quot;an axis may read the base config&quot; — was implemented, tried, and abandoned, because it needs two incompatible notions of order. For <code>[&quot;~~variants&quot;, {&quot;lr&quot;: 0.7}]</code>, evaluating the axis against all non-axis tokens gives <code>self.config.lr == 0.7</code> and then merges the element where the token sat, where the trailing patch overwrites it: the axis reads as if the patch came first and is overridden as if it came last. Evaluating against only the tokens to the <em>left</em> is self-consistent but makes <code>self.config</code> inside the axis differ from <code>.config</code> outside it, on the same expression. Neither is defensible, and the failure is not always caught: elements <code>[{&quot;lr&quot;: 0.07, &quot;tag&quot;: &quot;a&quot;}, {&quot;lr&quot;: 0.7, &quot;tag&quot;: &quot;b&quot;}]</code> under a trailing <code>{&quot;lr&quot;: 0.7}</code> stay distinct by <code>tag</code> while every <code>lr</code> is silently <code>0.7</code>.</p><p>Without <code>self</code> there is exactly one ordering rule — the element merges where the token is — and no config question at all. A relative sweep takes an argument (<code>~~variants(base=0.5)</code>), which is strictly more legible: the dependency is in the version list and round-trips through <code>to_cli()</code>, where <code>~sgd ~~variants</code> hid that <code>variants</code> read <code>~sgd</code>. A sweep that wants a config field supplies it (<code>~~sessions(root=/data)</code> yielding both <code>root</code> and <code>path</code>), so the parameter belongs to the axis rather than being duplicated between config and call site.</p><p>A proxy <code>self</code> that raises only on <code>.config</code> was considered as the targeted version of the same enforcement, and rejected: it blocks more than the rule, breaks <code>isinstance</code>, <code>save_file</code>/<code>load_file</code> and session pickling for any axis that touches them, and <code>@staticmethod</code> says the same thing in one visible word.</p><h2 id="the-boundary-against-aggregates" tabindex="-1">The boundary against aggregates <a class="header-anchor" href="#the-boundary-against-aggregates" aria-label="Permalink to &quot;The boundary against aggregates&quot;">​</a></h2><p>The <code>@staticmethod</code> rule closes the easy route to impurity — no <code>self</code>, hence no <code>load_file</code>, no <code>interfaces</code>, no config — but a module-level import can still read prior results, so the rest is contract:</p><ol><li><strong>Enumerable without running anything.</strong> Needing results to decide makes it an experiment, not an axis.</li><li><strong>Pure and deterministic</strong> given project state; group identity <em>is</em> the enumerated set, so sort your elements.</li><li><strong>Unordered and independent.</strong> Members that depend on each other need an aggregate and <code>uses</code>.</li><li><strong>No state of its own beyond arguments.</strong> The sharpest test: does the sweep&#39;s parameter need to survive as a record? Then it wants a <code>Config</code>, i.e. an aggregate.</li></ol><p>Compressed: an axis answers <em>which points?</em>, an aggregate answers <em>what happens?</em> — a set versus a procedure.</p><p>An adaptive axis that reads prior results is possible and unattractive: its group identity is unstable, expansion happens at launch time so the results must already exist and the sweep quietly becomes order-dependent, and the dependency is <strong>invisible to <a href="./provenance.html">provenance</a></strong> where an aggregate would record <code>uses</code> edges. The last is the real argument, and the reason the boundary is not merely stylistic.</p><h2 id="also-rejected" tabindex="-1">Also rejected <a class="header-anchor" href="#also-rejected" aria-label="Permalink to &quot;Also rejected&quot;">​</a></h2><ul><li><strong><code>get.each(module, [versions])</code> returning a collection.</strong> Caller-side only: the axis stays undeclared, unspeakable on the CLI, and unusable as an inference operand. Still reasonable as a later convenience for genuinely ad-hoc fan-out.</li><li><strong><code>get()</code> returning an <code>InterfaceCollection</code>.</strong> Breaks the return contract and loses both the operand story and the class&#39;s own methods.</li><li><strong><code>InterfaceCollection</code> inheriting from <code>Interface</code></strong>, so that a coordinator could simply <em>be</em> a collection. The most attractive wrong turn, and worth recording in full: four hard name collisions with incompatible semantics (<code>serialize</code>, <code>__eq__</code>, <code>launch</code>, <code>collect</code>); every <code>filter</code>/<code>map</code>/<code>related()</code> would construct two pydantic models and walk the MRO for a container that is pure plumbing; it inverts the <a href="./decisions.html#the-collection-clean-break">Collection clean break</a>; and it costs laziness and the class&#39;s methods. The <code>__iter__</code>/<code>__len__</code> delegation on an expanded interface is the bounded version of what it was reaching for.</li><li><strong><code>{&quot;path&quot;: [&quot;a&quot;, &quot;b&quot;]}</code> meaning fan-out.</strong> Collides irrecoverably with list-typed config fields.</li><li><strong>A materialized group record.</strong> Inconsistent with ephemeral aggregates; anyone who wants a stored group handle can build one explicitly, since a coordinator is an ordinary interface.</li><li><strong>The prefixes <code>versions_</code> and <code>each_</code>.</strong> The first is a lie once contexts are elements; the second is a verb where <code>config_</code>/<code>version_</code> are nouns, and collides with <code>Collection.each</code>.</li></ul><h2 id="tension-with-no-sweep-dsl" tabindex="-1">Tension with &quot;no sweep DSL&quot; <a class="header-anchor" href="#tension-with-no-sweep-dsl" aria-label="Permalink to &quot;Tension with &quot;no sweep DSL&quot;&quot;">​</a></h2><p><a href="./philosophy.html#code-all-the-way-down">The philosophy</a> says machinable has no <code>sweep()</code> DSL, and that stands: an axis is a Python method returning a list, and the loop is still Python. What is new is only that a version token may denote several versions.</p><p>It does mean there are two ways to express a grid. The line: an axis for the flat case (same interface, N inputs), an aggregate when the coordinator has its own config, dependent ordering, or nested contexts. They compose — an aggregate&#39;s <code>launch()</code> may use axes, and an axis member may be an aggregate.</p><p>The honest residue is that axes are not content-addressed. A glob that picks up a new file changes the group, which is exactly why the group&#39;s identity is the set it produced rather than the token that produced it.</p>',40)])])}const g=t(i,[["render",s]]);export{p as __pageData,g as default};
