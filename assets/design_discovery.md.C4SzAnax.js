import{_ as t,o as a,c as o,ag as i}from"./chunks/framework.Nx1zvZTu.js";const u=JSON.parse('{"title":"Discovery","description":"","frontmatter":{},"headers":[],"relativePath":"design/discovery.md","filePath":"design/discovery.md"}'),r={name:"design/discovery.md"};function s(n,e,c,d,h,l){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="discovery" tabindex="-1">Discovery <a class="header-anchor" href="#discovery" aria-label="Permalink to &quot;Discovery&quot;">​</a></h1><p>How to list a project&#39;s interfaces from the CLI, the API, or an editor is in the <a href="/guide/discovery.html">guide</a>. This note records why discovery is shaped the way it is.</p><p>Discovery answers &quot;what interfaces does this project offer, and how are they configured&quot;. This is useful for autocompletion, the API&#39;s module catalog, and any tool that needs the project&#39;s vocabulary before anything runs. The answer has to be cheap and safe, i.e. typing <code>machinable &lt;tab&gt;</code> must not wait on a heavy import, or execute arbitrary project code just to render a list.</p><h2 id="the-governing-principle" tabindex="-1">The governing principle <a class="header-anchor" href="#the-governing-principle" aria-label="Permalink to &quot;The governing principle&quot;">​</a></h2><p>The obvious way to describe an interface is to import it and introspect the class, but importing a research file runs its top-level side effects and pulls its heavy dependencies. Moreover, the walk that finds those files descends into whatever the directory skip-set does not prune (e.g. <code>venv</code>, a build tree, a gitignored scratch dir). To avoid this, the discovery&#39;s guiding principle is:</p><blockquote><p>Discovery is a read-only, best-effort static analysis. It imports nothing and promises nothing.</p></blockquote><p>When static analysis cannot resolve a construct (base class computed at runtime, a <code>Config</code> built by a metaclass, a <code>*</code>-import, etc.), the discovery surfaces what it could extract and marks the rest <code>partial</code>. It does not fall back to importing to recover fidelity. A project that reaches for dynamic tricks gets degraded discovery as the signal to write conventionally. machinable does not execute code to guarantee correctness it said it would not guarantee. This is the same stance <a href="./provenance.html">provenance</a> takes toward an unresolvable manifest where the node still exists and still describes itself, without the bytes.</p><h2 id="discovery-is-an-interface-kind" tabindex="-1">Discovery is an interface kind <a class="header-anchor" href="#discovery-is-an-interface-kind" aria-label="Permalink to &quot;Discovery is an interface kind&quot;">​</a></h2><p>Discovery follows the <a href="./provenance.html"><code>Manifest</code></a>/<a href="./storage.html"><code>Storage</code></a> motif in that the base <code>Discovery</code> is the local implementation using a read-only AST walk of the project source that honors <code>.gitignore</code>. Other schemes (a remote catalog, a package registry, a different VCS&#39;s ignore file) are ordinary <code>Discovery</code> subclasses, as the <a href="./philosophy.html">philosophy</a> demands. A project declares its schemes from its provider via <code>on_resolve_discovery()</code>.</p><p>Because <code>Discovery</code> is a real interface, it inherits Config, versions, and config methods for free, and it offers two override seams at different grains: the ignore rule, so a Mercurial user swaps in <code>.hgignore</code> by overriding one method, and the walk itself, for a wholly different source such as a remote catalog. Extra ignore patterns are a Config value, tunable with no subclass at all. <code>Discovery</code> is never entered as a <code>with</code>-context, it is resolved on demand and instantiated per call, like <code>Manifest</code>, so it never touches the connection stack.</p><h2 id="resolving-inheritance-without-importing" tabindex="-1">Resolving inheritance without importing <a class="header-anchor" href="#resolving-inheritance-without-importing" aria-label="Permalink to &quot;Resolving inheritance without importing&quot;">​</a></h2><p>The hard part of static discovery is that &quot;is this an <code>Interface</code>?&quot; and &quot;what is its full <code>Config</code>?&quot; both depend on a class&#39;s base chain, which crosses module boundaries. machinable already records that chain at runtime as <code>inherits</code> (the MRO module tuple) so discovery reconstructs it statically with a symbol graph. Every candidate file is parsed once, and an import table (resolving aliased and relative imports to a concrete module and symbol) lets base references resolve class to class, across files.</p><p>The graph is anchored on machinable itself. machinable is installed and trusted, so its own interface kinds are enumerated by importing machinable only (no user code). A project class is an interface exactly when its resolved base chain terminates at one of those anchors. Only user and remote modules are parsed. A chain that terminates at an unresolvable symbol like a third-party base, a star-import, etc. is not confirmed, so the module is either dropped or surfaced <code>partial</code>. The same walk merges an inherited or extended <code>Config</code> (a subclass that extends the parent&#39;s <code>Config</code>, or omits it to inherit outright) by collecting fields along the resolved chain, child overriding parent.</p><h2 id="what-gets-extracted" tabindex="-1">What gets extracted <a class="header-anchor" href="#what-gets-extracted" aria-label="Permalink to &quot;What gets extracted&quot;">​</a></h2><p>Because a <code>Config</code> must subclass <code>pydantic.BaseModel</code>, the static side handles exactly one shape. From a confirmed class it reads the config fields (with their type, whether they are required, a literal default when one can be evaluated, and the non-identifying flag), the <code>version_*</code> vocabulary with signatures and docstrings, and the <code>config_*</code> methods. Anything the parser cannot evaluate is skipped rather than guessed, and flips the result to <code>partial</code> as an advisory hint carried through the API, MCP, and CLI.</p><h2 id="remotes-carry-their-declared-name" tabindex="-1">Remotes carry their declared name <a class="header-anchor" href="#remotes-carry-their-declared-name" aria-label="Permalink to &quot;Remotes carry their declared name&quot;">​</a></h2><p>A remote is declared through <code>on_resolve_remotes()</code>, a map from a declared module name to a source, where the key is the identity-relevant name and the on-disk location under <code>interface/remotes/</code> is an implementation detail that never enters dedup. Discovery preserves that distinction in that it reports a remote under its declared name, using the physical path only to locate source for the parser. Vendored code is walked solely to populate the symbol graph so cross-boundary inheritance resolves but never names a discovered module.</p><p>Discovery is static, so it never fetches. A declared-but-unfetched remote is listed under its declared name with an empty <code>partial</code> schema (known to exist, not yet introspectable). <code>machinable fetch</code> remains the explicit step that makes the full schema available.</p><h2 id="the-safety-floor" tabindex="-1">The safety floor <a class="header-anchor" href="#the-safety-floor" aria-label="Permalink to &quot;The safety floor&quot;">​</a></h2><p>A non-negotiable floor sits below the pluggable ignore layer so that dot-directories (<code>.git</code>, <code>.venv</code>), <code>__pycache__</code>, <code>node_modules</code>, and the storage/scratch trees are pruned regardless of any scheme. The base gitignore semantics sit on top of that floor.</p>',20)])])}const m=t(r,[["render",s]]);export{u as __pageData,m as default};
