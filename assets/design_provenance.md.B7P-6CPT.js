import{_ as a,o as s,c as t,ag as i}from"./chunks/framework.Nx1zvZTu.js";const k=JSON.parse('{"title":"Provenance","description":"","frontmatter":{},"headers":[],"relativePath":"design/provenance.md","filePath":"design/provenance.md"}'),n={name:"design/provenance.md"};function o(r,e,d,h,l,c){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="provenance" tabindex="-1">Provenance <a class="header-anchor" href="#provenance" aria-label="Permalink to &quot;Provenance&quot;">​</a></h1><p>How to read a run&#39;s provenance is in the <a href="/guide/provenance.html">guide</a>. This note records why the provenance subsystem is shaped the way it is.</p><p>Provenance is machinable&#39;s answer to &quot;what exactly produced this result, and how do I get it back&quot;. For a system whose pitch is <a href="./identity.html">content-addressed reproducibility</a>, that answer has to be captured at run time. Provenance reconstructed at read time, against whatever code happens to be checked out months later, is provenance that can be wrong.</p><h2 id="one-graph-several-projections" tabindex="-1">One graph, several projections <a class="header-anchor" href="#one-graph-several-projections" aria-label="Permalink to &quot;One graph, several projections&quot;">​</a></h2><p><code>provenance</code>, <code>lineage</code>, <code>related</code>, and <code>ancestor</code> are not four features. They are projections over one typed interface graph, and machinable already is that graph: the <code>@has_many</code>/<code>@belongs_to</code> relations persist to a SQLite edge table with a disk mirror (<code>related/metadata.jsonl</code>). Provenance adds a single traversal and serialization layer on top, rather than a parallel data model.</p><p>The graph has two levels:</p><ul><li><strong>Recipe</strong>: interface-level, deterministic, content-addressed. What the interface <em>is</em>: config layers, the <code>with</code>-context stack, derivation ancestry. (Inheritance is structural type information, not a per-instance input; see naming below.)</li><li><strong>History</strong>: an event that happened <em>to</em> a recipe. What was actually computed: each execution, with its engine, resources, seed, and timing, and the code it ran against.</li></ul><p>The split dictates where each piece lives. Recipe data sits on the interface and must be identity-neutral (like <code>created_by</code> and <code>label</code>), because recording it must not change the hash and break dedup. History data sits on the <code>Execution</code> record, where it is free to vary per run.</p><h2 id="the-governing-principle" tabindex="-1">The governing principle <a class="header-anchor" href="#the-governing-principle" aria-label="Permalink to &quot;The governing principle&quot;">​</a></h2><p>One rule settles the hard cases:</p><blockquote><p>machinable holds opaque references and never adjudicates their meaning.</p></blockquote><p>It is the same rule that governs <a href="./identity.html">config identity</a> (the canonical form is machinable&#39;s; the contents of free-form fields are hashed verbatim) and <a href="./storage.html">storage</a> (a remote URI is opaque; the backend interprets it). Provenance extends it to code.</p><h2 id="what-gets-persisted" tabindex="-1">What gets persisted <a class="header-anchor" href="#what-gets-persisted" aria-label="Permalink to &quot;What gets persisted&quot;">​</a></h2><p>Two pieces of data are captured beyond the relations that already exist, both identity-neutral:</p><p><strong>The context stack.</strong> At <code>materialize()</code>, the ordered <code>[(module, compact_version)]</code> of the entered <code>with</code>-contexts is recorded as metadata on the interface. Contexts fold into the <a href="./identity.html">predicate</a> at creation time, but the predicate is a hash; recording the stack makes it invertible, so the element chain a dispatch actually ran can be reconstructed by uuid without touching identity.</p><p><strong>Code provenance as a <code>Manifest</code> kind.</strong> Code, dependencies, and environment are captured as content-addressed <code>Manifest</code> nodes that an execution <code>uses</code>. Making it a graph node rather than a field on the execution record means it dedups, so the same code state resolves to one <code>Manifest</code>, shared by every run that matches, so &quot;show me every execution at this code state&quot; is an ordinary relation query. A per-run field would duplicate exactly the fact content-addressing exists to record.</p><p><code>Manifest</code> follows the <a href="./storage.html"><code>Storage</code></a> motif: the base <code>Manifest</code> is the git implementation, the way the base <code>Storage</code> is the local-filesystem backend. Other tools (a dependency lock, a container digest) are ordinary subclasses that override <code>capture()</code> and, as needed, <code>resolve</code>/<code>verify</code>/<code>describe</code>. There is no registry; a tool is available because it is importable, as <a href="./philosophy.html">philosophy</a> demands. A project declares its tools via <code>Project.on_resolve_manifests()</code> (default: the git manifest), the same <code>on_resolve_*</code> motif as <code>on_resolve_remotes</code>. A run therefore <code>uses</code> one manifest node per resolved tool, each independently content-addressed.</p><p>A manifest holds a list of entries <code>{provider, component, token}</code> where the token is opaque to machinable. There are no git-shaped <code>commit</code>/<code>diff</code> fields; git is one tool among many. Identity is <code>hash(entries)</code>. Because tokens are opaque, the awkward cases fall to the tool:</p><ul><li>A dirty tree is the git manifest&#39;s business. Capture is strictly read-only: the tool records the commit, a dirty flag, and a fingerprint of the uncommitted state (the tracked diff plus untracked <em>paths</em>, never file contents), so identical dirty states dedup to one node while the repository is never written to. That trades restorability of uncommitted changes for safety; capture stays cheap with large untracked data (a freshly downloaded dataset not yet gitignored) and race-free under concurrent capture (MPI ranks).</li><li>An unresolvable manifest is still a first-class node. A collaborator&#39;s run may name a commit you cannot reach; the node still exists and still <code>describe()</code>s itself, with <code>resolve()</code>/<code>verify()</code> succeeding only where the tool can reach the bytes. This is the stance storage takes with remote URIs: provenance survives without the bytes.</li></ul><p>Capture happens at <code>dispatch_interface</code>, per run, so a resume gets its own manifest.</p><h2 id="naming" tabindex="-1">Naming <a class="header-anchor" href="#naming" aria-label="Permalink to &quot;Naming&quot;">​</a></h2><ul><li><code>lineage</code> used to mean the class-MRO chain, which readers perennially confused with derivation. The MRO chain is now <code>inherits</code>, and <code>lineage()</code> means the derivation ancestor chain users expect.</li><li><code>uses</code> stays adjacency, not recipe.</li><li><code>Manifest</code> was chosen over <code>Snapshot</code> because it holds references (a bill of materials), not bytes, which is honest about the delegation boundary. It deliberately rhymes with the <code>predicate_from_manifest</code> helper, as both are content identities that re-establish something.</li></ul><h2 id="the-wire-shape" tabindex="-1">The wire shape <a class="header-anchor" href="#the-wire-shape" aria-label="Permalink to &quot;The wire shape&quot;">​</a></h2><p>The provenance graph is a DAG, not a tree. <code>context</code>, <code>uses</code>, and <code>manifest</code> edges are many-to-many, and the shared nodes (a manifest used by forty runs) are the payload; a tree would duplicate them. The walk therefore serializes as a normalized node-link structure: nodes keyed once, typed edges by reference.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GraphNode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">BaseModel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    uuid: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    kind: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                       # Interface | Execution | Manifest | Scope ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    module: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    version: list[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dict</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> []</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    label: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> None</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    attributes: dict[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Any] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {} </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># per-kind facet</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GraphEdge</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">BaseModel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    source: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    target: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    rel: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                        # derivation | context | uses | runs | manifest</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProvenanceGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">BaseModel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    root: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">str</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    nodes: list[GraphNode]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    links: list[GraphEdge]</span></span></code></pre></div><p>One envelope serves every surface: <code>Interface.provenance()</code>, the REST route, and the MCP tool all return this object. <code>related</code> is its depth-1 slice. The node-link JSON convention was adopted because d3, cytoscape, and <code>networkx.node_link_graph</code> read it directly. machinable does not take a graph library as a dependency, since the operations provenance needs are a bounded BFS with a seen-set, an edge filter, and a depth cap.</p><h2 id="traversal-policy" tabindex="-1">Traversal policy <a class="header-anchor" href="#traversal-policy" aria-label="Permalink to &quot;Traversal policy&quot;">​</a></h2><p>The default walk follows <code>derivation</code>, <code>runs</code>, and <code>manifest</code> deeply (trees or single hops, cheap) and expands <code>uses</code> exactly one hop, so you see what a run directly consumed without paying for its transitive closure. Deeper walks are opt-in via <code>depth=</code>/<code>rels=</code>. A node cap bounds the output and sets an explicit <code>truncated</code> flag, so the default stays bounded and honest about being bounded rather than silently dropping edges.</p><p><code>Manifest</code> identity never models &quot;partial&quot; capture. A manifest is exactly its entries; git-only and git+container are simply different manifests that dedup differently. A completeness flag would require machinable to know the expected tool set, which is the governing principle&#39;s line: it has no grounds to know. A tool that runs and finds nothing can emit an explicit sentinel entry (a real, hashable token); &quot;warn me the environment wasn&#39;t captured&quot; is a read-time comparison against the currently resolved tool set, the same shape as storage&#39;s <code>bytes_missing</code>, never baked into the content hash.</p>`,29)])])}const u=a(n,[["render",o]]);export{k as __pageData,u as default};
