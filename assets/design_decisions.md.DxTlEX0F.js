import{_ as o,o as a,c as i,ag as n}from"./chunks/framework.Nx1zvZTu.js";const p=JSON.parse('{"title":"Key decisions","description":"","frontmatter":{},"headers":[],"relativePath":"design/decisions.md","filePath":"design/decisions.md"}'),d={name:"design/decisions.md"};function c(t,e,s,r,l,h){return a(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="key-decisions" tabindex="-1">Key decisions <a class="header-anchor" href="#key-decisions" aria-label="Permalink to &quot;Key decisions&quot;">​</a></h1><p>A few smaller design decisions that shaped the current API, recorded so the reasoning isn&#39;t lost.</p><h2 id="pydantic-only-config" tabindex="-1">pydantic-only <code>Config</code> <a class="header-anchor" href="#pydantic-only-config" aria-label="Permalink to &quot;pydantic-only `Config`&quot;">​</a></h2><p><a href="/guide/configuration.html"><code>Config</code> must be a pydantic <code>BaseModel</code></a>; plain <code>dict</code> and dataclass configs were dropped and now raise. The reason is <a href="./identity.html">identity</a>: typed fields coerce (<code>1</code> becomes <code>1.0</code>), which is what makes the canonical form well-defined. An untyped dict config lets the same value reach the hash as <code>1</code> in one record and <code>1.0</code> in another, producing spurious distinct identities, exactly the subtlety the canonical form exists to remove. With identity riding on the schema, requiring pydantic is a prerequisite rather than a tax, and it pays for itself (validation, defaults, <code>model_fields</code>, JSON-schema for the API and MCP). The only un-coerced residue is the contents of free-form container fields, which are hashed verbatim by design.</p><h2 id="cloudpickle-for-in-session-interfaces" tabindex="-1">cloudpickle for in-session interfaces <a class="header-anchor" href="#cloudpickle-for-in-session-interfaces" aria-label="Permalink to &quot;cloudpickle for in-session interfaces&quot;">​</a></h2><p>An ad-hoc interface defined in a REPL or notebook (a <code>__session__</code> module) is persisted by pickling its class. <code>dill</code> cannot pickle a locally-defined pydantic model class, so making <code>Config</code> pydantic-only would have broken in-session interfaces outright. The class dump uses <code>cloudpickle</code> instead (by value, and the stream stays standard-<code>pickle</code>-loadable). Module-level interfaces were never affected, since they pickle by reference. A small dependency in exchange for keeping notebook-defined interfaces working.</p><h2 id="the-collection-clean-break" tabindex="-1">The <code>Collection</code> clean break <a class="header-anchor" href="#the-collection-clean-break" aria-label="Permalink to &quot;The `Collection` clean break&quot;">​</a></h2><p><a href="/guide/collections.html"><code>Collection</code></a> was reduced to a minimal live-run handle (<code>filter</code>/<code>map</code>/<code>first</code>/<code>last</code>/relations/<code>launch</code>/<code>find_many_by_id</code>); the home-brewed analytics methods (<code>pluck</code>, <code>avg</code>, <code>implode</code>, <code>as_numpy</code>, ...) were removed, not soft-deprecated. The reasoning: a <code>Collection</code> holds live, behavior-bearing interfaces while analysis wants tabular values, which are different kinds of object. Rather than reimplement pandas in idioms nobody has training on, machinable hands off a DataFrame via a flat-columned <code>as_dataframe()</code> and keeps <code>pandas</code> a lazy import out of core. This reduces the bespoke surface to learn instead of mimicking pandas.</p><h2 id="unknown-config-keys" tabindex="-1">Unknown config keys <a class="header-anchor" href="#unknown-config-keys" aria-label="Permalink to &quot;Unknown config keys&quot;">​</a></h2><p>As a consequence of strict pydantic models, an unknown config key (a typo in a version) is silently dropped by pydantic&#39;s default <code>extra=&quot;ignore&quot;</code> rather than creating a distinct run. This is mostly desirable (schema discipline) but silent; whether to switch to <code>extra=&quot;forbid&quot;</code> so typos raise is an open follow-up.</p>',10)])])}const u=o(d,[["render",c]]);export{p as __pageData,u as default};
