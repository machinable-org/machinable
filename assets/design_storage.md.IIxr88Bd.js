import{_ as t,o as i,c as a,ag as n}from"./chunks/framework.Nx1zvZTu.js";const f=JSON.parse('{"title":"Storage","description":"","frontmatter":{},"headers":[],"relativePath":"design/storage.md","filePath":"design/storage.md"}'),o={name:"design/storage.md"};function r(s,e,d,c,l,h){return i(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="storage" tabindex="-1">Storage <a class="header-anchor" href="#storage" aria-label="Permalink to &quot;Storage&quot;">​</a></h1><p>The mechanics of <a href="/guide/storage.html">storage and the index</a> are in the guide. The design choice worth recording is the strict separation of location from identity.</p><h2 id="location-identity" tabindex="-1">Location ≠ identity <a class="header-anchor" href="#location-identity" aria-label="Permalink to &quot;Location ≠ identity&quot;">​</a></h2><p>A run&#39;s directory is a location. Its identity is content-addressed (<a href="./identity.html">canonical config</a> + predicate) and entirely independent of where the directory sits. This is deliberate, and it enables several things:</p><ul><li><strong>The index is a cache, not the source of truth.</strong> Each run directory carries its own <code>model.json</code> (module, config, version, predicate, inherits, context) and, since <a href="./format.html">format v1</a>, an <code>id.json</code> identity header. The SQLite index maps identities to locations for fast lookup and search, but it can always be rebuilt from the directories. Move a results folder, copy it to a cluster, hand it to a collaborator, check it out fresh: <code>reindex()</code> restores every identity and the catalog is whole again.</li><li><strong>Reindex copies; verify re-derives.</strong> Identity keys are persisted in <code>id.json</code> at materialization, so a rebuild is a pure copy with no <code>Config</code> import, implementable in any language (<a href="./format.html">the directory format</a>). The schema-consulting re-derivation (which fields are non-identifying, what the defaults are) lives in <code>verify</code>/<code>repair</code>, where consulting the current code as the source of identity semantics is the point: detecting drift, rather than silently re-keying a catalog on a machine where the project happens not to import.</li><li><strong>Data can be elsewhere.</strong> A run can be local, in a remote store, or indexed-but-evicted. Separating identity from location lets machinable track availability and fetch on demand, and lets storage backends (mirrors like Globus/Aimstack) be ordinary interfaces you connect as a context.</li></ul><h2 id="why-not-store-identity-in-the-index-alone" tabindex="-1">Why not store identity in the index alone? <a class="header-anchor" href="#why-not-store-identity-in-the-index-alone" aria-label="Permalink to &quot;Why not store identity in the index alone?&quot;">​</a></h2><p>Putting identity only in the database would make the database load-bearing: lose it and you lose the meaning of the directories. By writing <code>model.json</code> next to the data, the directories remain self-describing and portable, and the index is a rebuildable convenience. This is the same instinct as content-addressing the config: keep the durable truth in the artifact, not in a mutable side-channel.</p>',7)])])}const m=t(o,[["render",r]]);export{f as __pageData,m as default};
