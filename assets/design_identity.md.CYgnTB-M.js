import{_ as t,o as a,c as i,ag as n}from"./chunks/framework.Nx1zvZTu.js";const g=JSON.parse('{"title":"Config identity","description":"","frontmatter":{},"headers":[],"relativePath":"design/identity.md","filePath":"design/identity.md"}'),o={name:"design/identity.md"};function r(s,e,d,l,c,h){return a(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="config-identity" tabindex="-1">Config identity <a class="header-anchor" href="#config-identity" aria-label="Permalink to &quot;Config identity&quot;">​</a></h1><p>How <a href="/guide/identity.html">identity &amp; dedup</a> works is in the guide. This note records why it is built the way it is, and specifically why identity hashes a canonical normal form of the configuration rather than the configuration itself.</p><h2 id="the-problem-with-hashing-the-resolved-config" tabindex="-1">The problem with hashing the resolved config <a class="header-anchor" href="#the-problem-with-hashing-the-resolved-config" aria-label="Permalink to &quot;The problem with hashing the resolved config&quot;">​</a></h2><p>An earlier design hashed the fully-resolved configuration. Two failures followed:</p><ol><li><strong>Schema evolution.</strong> Add a field with a default, or rename one, and every prior record&#39;s resolved config shifts, so its hash shifts, and <code>get(...)</code> no longer finds it. Records silently disappeared whenever a <code>Config</code> changed.</li><li><strong>Volatile location.</strong> An interface computing over an external blob it doesn&#39;t own got a different identity per data URI (<code>file://</code> on the rig, <code>s3://</code> on the cluster), so moving the data spuriously recomputed.</li></ol><p>Both are the same mistake: identity was computed over the materialized object, incidental detail included.</p><h2 id="borrowing-from-content-addressed-languages" tabindex="-1">Borrowing from content-addressed languages <a class="header-anchor" href="#borrowing-from-content-addressed-languages" aria-label="Permalink to &quot;Borrowing from content-addressed languages&quot;">​</a></h2><p>The fix follows how content-addressed systems handle this:</p><ul><li><strong>Dhall</strong> hashes the αβ-normal form. It evaluates and canonicalizes, so equivalent expressions hash identically regardless of how they&#39;re written. Hash a normal form, not syntax.</li><li><strong>dCBOR / protobuf</strong> omit default values from the canonical form; a field at its default is indistinguishable from an absent one. A default contributes nothing to identity.</li><li><strong>Unison</strong> content-addresses code structure and keeps names as separate, migratable metadata, so renames never break references. Address a stable essence; keep mutable presentation (names, URIs, labels) separate.</li></ul><h2 id="the-canonical-normal-form" tabindex="-1">The canonical normal form <a class="header-anchor" href="#the-canonical-normal-form" aria-label="Permalink to &quot;The canonical normal form&quot;">​</a></h2><p>Identity is <code>hash(module + canonical(config))</code>, where <code>canonical</code> is: evaluate (expand <code>~versions</code> and config methods), strip non-identifying fields (<code>Field(identifying=False)</code>), recursively drop keys equal to their default (descending into nested models), then serialize as sorted, compact JSON. pydantic&#39;s coercion does the numeric folding (<code>1</code> ≡ <code>1.0</code>) upstream, so no separate pass is needed.</p><p>The payoffs map back to the failures:</p><ul><li><strong>Version spelling is UX, not identity.</strong> Two spellings that evaluate to the same config share an identity. The version layer is how you dial in a config; it can be renamed or restructured while the underlying research stays stable.</li><li><strong>Schema evolution is safe.</strong> Adding a config field with a default leaves prior records&#39; canonical form unchanged, so they remain reachable.</li><li><strong>Volatile location is solved</strong> by letting a field leave identity (<code>Field(identifying=False)</code>) while a content predicate (<code>predicate_from_manifest</code>) re-identifies the data by a stable id.</li></ul><h2 id="references-not-by-value" tabindex="-1">References, not by-value <a class="header-anchor" href="#references-not-by-value" aria-label="Permalink to &quot;References, not by-value&quot;">​</a></h2><p>Complex or environment-dependent things (a factory, a dtype map, another interface) enter the canonical form by reference (the <code>(module, version)</code> element form), never by value. A reference identifies by name + args, not by the behavior of the referenced code. This is the Unison caveat: rename is safe, repointing is invisible, so use a predicate if you need behavior-identity. Hashing arbitrary objects by value is exactly the fragility the canonical design avoids.</p><h2 id="matching-follows-identity" tabindex="-1">Matching follows identity <a class="header-anchor" href="#matching-follows-identity" aria-label="Permalink to &quot;Matching follows identity&quot;">​</a></h2><p>The lookup path (<code>find</code>/<code>singleton</code>) matches on the same canonical <code>identity_key</code>, not on the literal version, so a record stored under one spelling is found by any equivalent spelling. The dedup tuple is <code>(parent_id, identity_key, predicate_key)</code>; relations never affect it.</p>',17)])])}const m=t(o,[["render",r]]);export{g as __pageData,m as default};
