import{_ as t,o as n,c as a,ag as i}from"./chunks/framework.Nx1zvZTu.js";const p=JSON.parse('{"title":"Inference design","description":"","frontmatter":{},"headers":[],"relativePath":"design/inference.md","filePath":"design/inference.md"}'),o={name:"design/inference.md"};function s(r,e,c,d,h,l){return n(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="inference-design" tabindex="-1">Inference design <a class="header-anchor" href="#inference-design" aria-label="Permalink to &quot;Inference design&quot;">​</a></h1><p>How to write and use an <a href="/guide/inference.html">inference</a> is in the guide. This note records why the inference subsystem is shaped the way it is.</p><h2 id="inferences-not-a-metrics-schema" tabindex="-1">Inferences, not a metrics schema <a class="header-anchor" href="#inferences-not-a-metrics-schema" aria-label="Permalink to &quot;Inferences, not a metrics schema&quot;">​</a></h2><p>The question that started it: how should a code agent and a researcher collaborate on a research question (&quot;is SGD faster than Adam?&quot;) without bolting an experiment-tracking schema onto machinable?</p><p>The tempting answer is a generic <code>compare(query, metric, group_by)</code> plus a <code>log_metric</code> convention. We rejected it because it forces a metrics schema, which means foreseeing what a measurement is. Instead, the MCP offers a vocabulary of scientific inferences, the questions a researcher actually asks (<code>outperforms</code>, <code>beats_baseline</code>, <code>ranks</code>, <code>converges_faster</code>), and pushes everything else down into code. There is no <code>Interface.evidence</code> method, no fixed metric enum, and no core resolution logic.</p><h2 id="the-contract-is-owned-by-the-inference-the-numbers-by-the-operand" tabindex="-1">The contract is owned by the inference; the numbers by the operand <a class="header-anchor" href="#the-contract-is-owned-by-the-inference-the-numbers-by-the-operand" aria-label="Permalink to &quot;The contract is owned by the inference; the numbers by the operand&quot;">​</a></h2><p>An inference declares the shape of evidence it needs (<code>scalar</code> or <code>series</code>) and resolves it by calling the operand, not by reading a fixed file. The operand satisfies the contract with a plain <code>&lt;quantity&gt;()</code> accessor returning one run&#39;s measurement. The inference composes that across the operand&#39;s runs into the sample its test needs.</p><p>This split is the whole design: the operand supplies the numbers, the inference supplies the test (bringing its own scipy), and machinable supplies neither, only identity, dedup, storage, and search. A statistical choice is an implementation, shareable like any interface; machinable ships no statistics.</p><h2 id="the-notimplementederror-is-the-engine" tabindex="-1">The <code>NotImplementedError</code> is the engine <a class="header-anchor" href="#the-notimplementederror-is-the-engine" aria-label="Permalink to &quot;The `NotImplementedError` is the engine&quot;">​</a></h2><p>When you ask for a quantity an operand doesn&#39;t expose, <code>resolve</code> raises a <code>NotImplementedError</code> that names the accessor to write. This is not an error to handle; it is the teaching signal. It turns &quot;the agent must guess what to implement&quot; into &quot;the contract tells the agent what to write next&quot;, which is what makes the <a href="/mcp/workflow.html">agent loop</a> reliable. The same instinct runs through the MCP: import errors on <code>write_source</code> and schema reflection correct the agent in context.</p><h2 id="identity-method-×-operands" tabindex="-1">Identity = method × operands <a class="header-anchor" href="#identity-method-×-operands" aria-label="Permalink to &quot;Identity = method × operands&quot;">​</a></h2><p>An <code>Inference</code> is an <code>Interface</code>, parallel to <code>Slurm(Execution)</code>, where the subclass is the question, the <code>Config</code> is the method, and the operands are subjects (via <code>.of(...)</code>, not config). The subtlety is that operands must enter the predicate, by config identity rather than uuid. If the method were config-only and operands lived in a relation, the same method applied to A and to B would share one identity and collapse to a single cached verdict. Operands are typically ephemeral aggregates that are never materialized (so they have no uuid); their <code>catalog_identity_key</code> is stable and computable without materialization. Net identity is method × operand-identities, so re-asking the same question dedups; different operands or a different method are distinct verdicts.</p><h2 id="staleness-is-explicit-not-detected" tabindex="-1">Staleness is explicit, not detected <a class="header-anchor" href="#staleness-is-explicit-not-detected" aria-label="Permalink to &quot;Staleness is explicit, not detected&quot;">​</a></h2><p>A verdict caches on <code>(method, operands)</code>. If an operand&#39;s results change while its config doesn&#39;t, the cached verdict can go stale, and machinable does not try to detect this. During research, things change constantly and the researcher knows when a result is invalidated; the remedy is the same <code>cached(False)</code> + re-run that invalidates any cached interface. We deliberately do not hash operand result-state into the predicate.</p>',14)])])}const u=t(o,[["render",s]]);export{p as __pageData,u as default};
