{
    "private": true,
    "scripts": {
        "gen": "uv run .vitepress/pydocgen.py && uv run .vitepress/integrationsgen.py && uv run python -m machinable.api.docs && npm --prefix ../web/sdk install && npm --prefix ../web/sdk run build && node .vitepress/copysdk.mjs",
        "predev": "npm run gen",
        "dev": "vitepress dev",
        "prebuild": "npm run gen",
        "build": "vitepress build",
        "preview": "vitepress preview"
    },
    "devDependencies": {
        "vitepress": "^1.6.4"
    }
}
