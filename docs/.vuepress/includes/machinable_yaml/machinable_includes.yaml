# machinable.yaml
components:
  - test.example: $/example/included.yaml

# example/included.yaml
learning_rate: 1
alpha: 0.5

# .. will be parsed as

components:
  - test.example:
      learning_rate: 1
      alpha: 0.5
