# fooba.machinable.yaml
name: test_project.fooba
components::
  experiments.grandfurther=grandfurther:
  experiments.base^grandfurther:
    elephant: at_home
    nested:
      elephants: outside
  experiments.start_parent:
  experiments.start^experiments.start_parent:
    test: 123
  experiments.dependent^experiments.base:
    overwrite_me:
      keep: me
      nested: level
components:models:
  models.baseline:
    overwrite: orginal
  models.extended=extended:
    hello: world
    overwrite: extended
    referenced: ${.overwrite}
  models.config_inheritance^extended:
    hello: inheritance
  models.config_dependency_inheritance^vendor.bedrock.models.base:
    here: we_go
    deep_overwrite: successful
  models.chained_inheritance^models.config_dependency_inheritance:
    deep_overwrite: early
